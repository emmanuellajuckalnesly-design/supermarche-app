name: Build and Push Docker Image to Docker Hub

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/supermarche-app:latest
            ${{ secrets.DOCKER_USERNAME }}/supermarche-app:${{ github.sha }}

      - name: Image pushed successfully
        run: echo "✅ Image Docker pushée sur Docker Hub !"
```

### Étape 19.3.3 : Sauvegarde le fichier

1. Clique sur **File** → **Save** (Ctrl+S)

✅ **Le fichier est modifié !**

---

## **ÉTAPE 19.4 : Commit et Push**

1. Ouvre **GitHub Desktop**
2. Tu devrais voir la modification de `docker.yml`
3. Écris le message :
```
   